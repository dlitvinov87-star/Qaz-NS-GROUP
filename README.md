# Qaz NS GROUP — сайт компании

Инструкция для редактора контента. Все правки делаются через GitHub.

---

## Структура проекта (что где лежит)

```
├── index.html          — главная страница
├── catalog.html        — страница каталога
├── catalog.json        — ВСЕ ТОВАРЫ И КАТЕГОРИИ (главный файл для редактирования)
├── styles.css          — стили оформления (не трогать без необходимости)
├── JS/
│   ├── main.js         — скрипты главной страницы
│   └── catalog.js      — скрипты каталога
└── images/
    └── catalog/        — картинки товаров, разложены по папкам категорий
        ├── vetosh/
        ├── favorit/
        ├── ochki/
        ├── perchatki/
        └── ... (остальные категории)
```

---

## Как редактировать каталог

Все товары и категории хранятся в одном файле — **`catalog.json`**.

Откройте его на GitHub: нажмите на файл → кнопка карандаша (Edit) справа вверху.

### Как устроен catalog.json

```
{
  "categories": [
    {
      "id": "ochki",                          ← уникальный код категории (латиницей)
      "title": "Защитные очки",               ← название на сайте
      "icon": "🥽",                           ← иконка-эмодзи
      "imagesPath": "images/catalog/ochki/",  ← путь к папке с картинками
      "items": [                              ← список товаров внутри
        {
          "id": "ochki-tpk-100",              ← уникальный код товара (латиницей)
          "name": "Очки защитные ТПК-100",    ← название товара на сайте
          "image": "ochki-tpk-100.jpg"        ← имя файла картинки из папки выше
        }
      ]
    }
  ]
}
```

---

## Частые задачи

### 1. Изменить название или описание товара

1. Откройте `catalog.json` на GitHub
2. Найдите нужный товар по `"name"` (Ctrl+F → поиск по названию)
3. Измените текст в кавычках после `"name":`
4. Нажмите **Commit changes** внизу

Пример — было:
```
"name": "Очки защитные ТПК-100"
```
Стало:
```
"name": "Очки защитные ТПК-100 (тёмный, прозрачный, жёлтый)"
```

### 2. Добавить новый товар в существующую категорию

1. Загрузите картинку товара в нужную папку `images/catalog/<категория>/`
   - На GitHub: откройте папку → **Add file** → **Upload files**
   - Название файла — латиницей, без пробелов (например `ochki-novye.jpg`)
2. Откройте `catalog.json`
3. Найдите нужную категорию
4. В массиве `"items"` добавьте новую строку перед закрывающей `]`:

```
,
        {
          "id": "ochki-novye",
          "name": "Новые защитные очки",
          "image": "ochki-novye.jpg"
        }
```

**Важно:** перед `{` нового товара должна быть запятая `,` после предыдущего `}`.

### 3. Удалить товар

1. Откройте `catalog.json`
2. Найдите товар
3. Удалите весь блок от `{` до `}` включительно
4. Убедитесь, что запятые расставлены правильно (после последнего товара запятой быть не должно)

Пример — удаляем второй товар:
```
Было:
  { "id": "товар-1", "name": "Первый", "image": "1.jpg" },
  { "id": "товар-2", "name": "Второй", "image": "2.jpg" },
  { "id": "товар-3", "name": "Третий", "image": "3.jpg" }

Стало:
  { "id": "товар-1", "name": "Первый", "image": "1.jpg" },
  { "id": "товар-3", "name": "Третий", "image": "3.jpg" }
```

### 4. Заменить картинку товара

1. Загрузите новую картинку в папку `images/catalog/<категория>/`
2. В `catalog.json` измените значение `"image":` на имя нового файла

### 5. Добавить новую категорию

1. Создайте папку для картинок: `images/catalog/<новая-категория>/`
   - На GitHub: перейдите в `images/catalog/` → **Add file** → **Create new file**
   - В поле имени напишите `новая-категория/.gitkeep` (это создаст папку)
2. Загрузите картинки товаров в эту папку
3. В `catalog.json` добавьте новый блок в массив `"categories"`:

```json
,
    {
      "id": "novaya-kategoriya",
      "title": "Название категории",
      "icon": "📦",
      "imagesPath": "images/catalog/novaya-kategoriya/",
      "items": [
        {
          "id": "tovar-1",
          "name": "Название товара",
          "image": "foto-tovara.jpg"
        }
      ]
    }
```

### 6. Удалить категорию

1. Удалите весь блок категории из `catalog.json` (от `{` до `}`)
2. При желании удалите папку с картинками из `images/catalog/`

---

## Как редактировать главную страницу

Файл — **`index.html`**. Основные места для правок:

| Что изменить | Где искать в файле |
|---|---|
| Телефон | Поиск по `77779206964` |
| Email | Поиск по `szrti-kz@mail.ru` |
| Адрес | Поиск по `Тауелсиздик` или `QUORUM` |
| Часы работы | Поиск по `09:00` |
| Название компании | Поиск по `Qaz NS GROUP` |
| Текст в hero-секции | Поиск по `Широкий ассортимент` |
| Текст «О компании» | Поиск по `казахстанская торговая` |

---

## Правила работы с JSON (чтобы не сломать)

1. **Все тексты в двойных кавычках**: `"name": "Текст"` — правильно
2. **Запятые между элементами**: после каждого `}` кроме последнего в списке
3. **Нет запятой перед `]`**: последний элемент в массиве — без запятой
4. **id — только латиница, цифры, дефис**: `"id": "ochki-novye"` — правильно
5. **Имя файла картинки — точно как на диске**: регистр важен! `Foto.jpg` и `foto.jpg` — разные файлы

Если после правки каталог перестал работать — скорее всего ошибка в запятых или кавычках. Проверьте JSON на сайте [jsonlint.com](https://jsonlint.com): вставьте содержимое `catalog.json` и нажмите Validate.

---

## Эмодзи для иконок категорий

Скопируйте нужный эмодзи и вставьте в поле `"icon"`:

🧹 🎨 🥽 🖌️ 🗑️ 🔥 🧤 📦 📋 🪖 ⚙️ 👷 ⛑️ 😷 🔧 🛡️ 🧰 🏗️ 🔨 🧱

---

## Контакты для вопросов по сайту

Если что-то не получается — напишите разработчику.
